/** Free tier caps (Supabase `users` row uses `is_pro` / `plan` for Pro). */
export const FREE_HABIT_LIMIT = 3
export const FREE_GOAL_LIMIT = 3
export const FREE_TRAINING_MODULE_SLOTS = 2
/** Personal training library items on /training (Layer 2) for Free / local users. */
export const FREE_PERSONAL_TRAINING_LIBRARY_LIMIT = 3
