'use client'

/** First-visit dismissible tip (`localStorage`). Radix hover tooltips: `@/components/ui/tooltip`. */
export {
  FeatureIntroTooltip as Tooltip,
  type FeatureIntroTooltipProps as TooltipProps,
  type FeatureIntroTooltipPosition as TooltipPosition,
} from './feature-intro-tooltip'
